#include <bits/stdc++.h>

using namespace std;

string s;

int main() {
	cin << s;
	if(s.length() != 1 && s[0] != 'P' && s[1] != 'P') {
		printf("NP");
		return 0;
	}
	else if(s.length() == 1 && s[0] == 'P') {
		
	}
}